progress = true
encryption = "gpg"

[diff]
command = "delta"

[edit]
command = "nvim"

[git]
autoPush = true

[merge]
command = "nvim"
args = ["-d", '{{ "{{ .Destination }}" }}', '{{ "{{ .Source }}" }}', '{{ "{{ .Target }}" }}']

[gpg]
symmetric = true